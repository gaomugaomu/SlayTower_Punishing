# Git Workflow

Purpose: Enable multiple AI agents to modify the project safely.

Rules:

-   Main branch contains stable builds.
-   Feature branches are created for each task.
-   Every change requires a clear commit message.

Suggested branches: - feature/combat - feature/ui - feature/content -
bugfix/xxx

AI agents must review existing changes before editing.
